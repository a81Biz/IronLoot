# Registro Maestro de ADR — IronLoot

| Metadato | Valor |
|---|---|
| **Origen** | Reconstrucción documental (no existía un registro ADR; sólo el Delta Log de 11-Conventions y los `changes/*/design.md`) — hallazgo relacionado AUD (falta de trail ADR) |
| **Fuente** | `06-Backend`, `09-Security`, `11-Conventions`, `03-TRD`, `changes/*/design.md`, código |
| **Fecha** | 2026-07-23 · **actualizado 2026-07-29** (ADR-050 … ADR-055, PT-188) |
| **Documentos usados** | 03-TRD, 06-Backend, 09-Security, 11-Conventions, CHANGELOG |
| **Código usado** | `src/api`, `src/apps/*`, `docker-compose.yml`, `prisma/` |
| **Nivel de confianza** | Alto (decisiones observadas en código); *Rationale* histórico marcado Medio donde procede |

> **2026-07-29 (PT-188).** Este registro se quedó en ADR-049 mientras se tomaban **seis** decisiones
> arquitectónicas más, ninguna citada aquí. Se añaden ADR-050 … ADR-055. Que un registro de decisiones no
> mencione ni un PT de la jornada en que se decidieron es la misma clase de omisión que ADR-049 vino a resolver:
> documentación que **parece** al día porque existe.

> Decisiones arquitectónicas **vigentes**, reconstruidas desde el código y la doc. Estado: **Aceptada** (en vigor) salvo indicación. Las decisiones con conflicto respecto a su implementación enlazan a un hallazgo.

| ADR | Decisión | Estado | Rationale / Consecuencia | Evidencia | Hallazgo |
|---|---|---|---|---|---|
| ADR-001 | **Monorepo multi-servicio** (api, base, client, admin, core) bajo `src/` | Aceptada | Aísla superficies pública/privada/admin; core comparte dominio sin HTTP/DB. Versionado no unificado. | CLAUDE.md, `E §6` | AUD-034 |
| ADR-002 | **NestJS + SSR Nunjucks** para los 3 frontends (sin framework SPA) | Aceptada | JS vanilla inline; no hay `public/js/pages/` como decía la convención. | `D §3` | AUD-030 |
| ADR-003 | **Patrón BFF**: JWT en cookie HttpOnly, proxy server-side inyecta `Authorization` | Aceptada (parcial) | Correcto en BASE; CLIENT carece de proxy para escrituras client-side. | `base/src/main.ts:73` | AUD-003 |
| ADR-004 | **JWT Bearer** como único mecanismo de auth del API (no cookies en API) | Aceptada | El API sólo lee `Authorization`. CLIENT revalida el JWT localmente con secreto compartido. | `jwt.strategy.ts:20` | AUD-026 |
| ADR-005 | **Admin con auth de sesión** server-side (Redis) + JWT/API-key de servicio hacia el API | Aceptada | Superficie separada; pero sin CSP/CSRF y con credenciales por defecto. | `admin/src/main.ts` | AUD-004, AUD-007 |
| ADR-006 | **Prisma + PostgreSQL**; migraciones versionadas | Aceptada (violada) | Se usó `db push` para el backoffice → 46% de modelos sin migración. | `C §5` | AUD-001 |
| ADR-007 | **Moneda única MXN**, `Decimal` no `Float` | Aceptada (parcial) | `payments.currency` default DB sigue `USD`; `Money` VO no usado. | `schema.prisma:304` | AUD-008, AUD-012 |
| ADR-008 | **Dominio en @ironloot/core** (FSM, validadores, VO, use-cases) framework-free | Aceptada (parcial) | Las FSM/validadores se reusan; los 4 use-cases y `Money` **no** se cablean. | `F §2` | AUD-012 |
| ADR-009 | **Cierre de subasta por cron con lock Redis distribuido** | Aceptada | Evita doble-procesamiento multi-instancia; captura y libera fondos en una TX. | `auction-scheduler.service.ts:45` | — |
| ADR-010 | **Soft-close configurable** (`AUCTION_SOFT_CLOSE_WINDOW_SEC`) | Aceptada | Anti-sniping; extiende `endsAt` en la ventana. | `bids.service.ts:100` | — |
| ADR-011 | **Config de negocio en SystemConfig** (seed desde env, override runtime en Admin) | Aceptada | Soft-close, moderación, incremento, KYC, verificación email. Nota: el incremento mínimo **sí se aplica** desde PT-042 (`bids.service.ts:92-98`); AUD-009 corregido. | `system-config.service.ts` | AUD-009 |
| ADR-012 | **Webhooks con validación de firma** (HMAC/IPN) antes de procesar | Aceptada | Nunca confiar en payload sin validar; rechaza si falta secreto. | `mercadopago.provider.ts:137` | — |
| ADR-013 | **Multi-proveedor de pago** (MercadoPago, PayPal, +HeyBanco, Stripe condicional) | Aceptada | HeyBanco no documentado; sólo MP+PayPal siempre activos. | `payments.service.ts:192` | AUD-023 |
| ADR-014 | **Ledger inmutable** append-only, correcciones vía ADJUSTMENT | Aceptada | Integridad financiera auditable. Retención de audit en conflicto. | `11-Conventions RULE-05` | AUD-018 |
| ADR-015 | **Real-time con Socket.io** (namespaces `auctions`/`events`) | Aceptada (incompleta) | Gateways sin auth; sin cliente WS en el frontend. | `auctions.gateway.ts` | AUD-002, AUD-006 |
| ADR-016 | **nginx como reverse proxy** por subdominio + traffic-switch en `ironloot.local` | Aceptada | Enruta base/api/admin/client; 301 por ruta en host bare. | `src/nginx/nginx.conf` | — |
| ADR-017 | **Rate limiting con @nestjs/throttler** (100/min global, estricto en auth/wallet/webhook) | Aceptada | Login admin queda `@SkipThrottle`. | `09-Security §7` | AUD-004 |
| ADR-018 | **Log de auditoría inmutable** (AuditEvent/ErrorEvent/RequestLog) con `traceId` | Aceptada | Observabilidad; retención vía cron (en conflicto). | `schema.prisma:460` | AUD-018 |
| ADR-019 | **Payout del vendedor manual detrás de `PayoutProvider`** (MVP `ManualPayoutProvider`; dispersión automática = Fase 2) | Aceptada | El banco ofrece cobros pero no dispersiones automáticas; se abstrae el mecanismo para enchufar SPEI/MP disbursement sin tocar la máquina de estados. El admin ejecuta la transferencia y marca PAID. | `wallet/payout/payout-provider.ts`, `withdrawals.service.ts` | PT-072 (RN-67) |
| ADR-020 | **Retención de liquidación (holdback) en `pendingBalance`**, liberada por entrega **o** vencimiento de disputa | Aceptada | Protege reembolsos/disputas: el neto de venta no es retirable hasta DELIVERED o `createdAt + DISPUTE_WINDOW_DAYS(14)`. Cron `EVERY_30_MINUTES` + asiento `SETTLEMENT_RELEASE`. | `wallet.service.ts`, `auction-scheduler.releaseMaturedSettlements` | PT-071 (RN-64) |
| ADR-021 | **KYC obligatorio** como puerta para vender y retirar | Aceptada | Cumplimiento AML/identidad: `enableSeller` y `withdrawals.request` exigen KYC APPROVED. Aprobación admin. Cierra OBS-01. | `kyc.service.ts`, `users.service.ts`, `withdrawals.service.ts` | PT-069 (RN-62) |
| ADR-022 | **Retiro con aprobación manual del admin** (REQUESTED→APPROVED→PAID/REJECTED), reserva de fondos al solicitar | Aceptada | Control antifraude antes de dispersar; el rechazo reintegra vía `ADJUSTMENT`. El viejo `/wallet/withdraw` inmediato delega ahora en la solicitud. | `withdrawals.service.ts`, `admin.controller.ts` | PT-072 (RN-65, RN-66) |
| ADR-023 | **PayPal migrado de WPS+IPN a Orders v2 API + Webhooks** | Aceptada | WPS/IPN es legacy y PayPal lo desaconseja para integraciones nuevas. Alinea PayPal con el patron REST+firma que ya usa MercadoPago en vez de mantener dos patrones divergentes. Introduce OAuth2 client_credentials con token cacheado, inexistente hasta ahora (MP usa token estatico). | `paypal.provider.ts` | PT-076 |
| ADR-024 | **La captura de Orders v2 ocurre dentro del webhook `CHECKOUT.ORDER.APPROVED`** | Aceptada | Orders v2 es en dos tiempos (crear -> aprobar -> capturar) y la interfaz compartida `PaymentProvider` no modela la captura. Capturar en la URL de retorno perderia pagos si el comprador cierra el navegador; anadir `capture()` a la interfaz obligaria a tocar los 4 proveedores. Se encapsula en el proveedor, como ya hace MercadoPago con su Orders API. | `paypal.provider.ts` (AD-01) | PT-076 |
| ADR-025 | **Idempotencia de webhooks por id de evento** (`processed_webhook_events`, unica `(provider, event_id)`) | **Sustituida por ADR-027** | PayPal reentrega hasta 25 veces en 3 dias hasta recibir un 2xx; sin deduplicacion cada reintento acreditaria de nuevo. Reserva-luego-acredita: `WalletService.deposit()` abre su propia transaccion y no admite cliente externo, asi que la unicidad es el punto de serializacion y la reserva se libera si la acreditacion falla. **Sustituida**: deduplicar por id de notificacion no cubre el caso de varias notificaciones distintas sobre un mismo pago (MercadoPago). Ver ADR-027. | `payments.service.ts:creditOnce`, `schema.prisma` | PT-076 |
| ADR-026 | **Los metodos de pago ofrecidos se derivan de la configuracion real** (`checkStatus()`) | Aceptada | Antes MERCADO_PAGO y PAYPAL estaban fijos como disponibles: la UI ofrecia PayPal aunque no estuviera configurado y el deposito reventaba al pulsar. Sustituye ADR-013 en el punto de "solo MP+PayPal siempre activos". | `payments.service.ts:getAvailableProviders`, `client/app.controller.ts` | PT-076 |
| ADR-027 | **Deduplicacion de webhooks por identificador de PAGO** (`processed_webhook_events`, unica `(provider, payment_id)`) | Aceptada | **Sustituye a ADR-025.** El id de notificacion solo protege del reintento de una misma notificacion; MercadoPago emite varias notificaciones distintas sobre el mismo pago (payment.created, payment.updated), cada una con su id. Solo el id de pago impide acreditar dos veces el mismo dinero. Se aplica a los cuatro proveedores. Fail-open si falta el id de pago: se prefiere un duplicado corregible por ADJUSTMENT a un deposito legitimo perdido. Los fallos de acreditacion pasan a propagar en todos los proveedores, porque responder 200 impedia el reintento y perdia el deposito en silencio. | `payments.service.ts:creditOnce`, `schema.prisma` | PT-078 (TD-006) |
| ADR-028 | **Mercado Pago notifica en DOS formatos y la validacion difiere por formato** | Aceptada | Webhooks (`data.id` en query) trae firma validable con el secret; IPN (`topic`+`id`) no, segun la propia documentacion de MP. El adaptador enrutaba por la FORMA del id (`/^(ORD|PAY)/i`) y rechazaba el formato IPN con 500: ningun pago real de Checkout Pro acreditaba. El discriminador correcto es el **topic**. En IPN la confirmacion contra la API es obligatoria y su respuesta es la unica fuente de verdad. | `mercadopago.provider.ts` | PT-080 (F-05) |
| ADR-029 | **Identificador canonico de pago por proveedor** | Aceptada | Un mismo cobro de MP existe como id de orden, id de pago de orden (`PAY...`) e id numerico. Solo el numerico resuelve en `/v1/payments/{id}`; `PAY...` da 404 alli y 400 en `/v1/orders`. Deduplicar por el id de orden permitia acreditar dos veces el mismo dinero. Cada adaptador resuelve siempre al mismo identificador, llegue por la ruta que llegue. | `mercadopago.provider.ts` | PT-080 (F-02) |
| ADR-030 | **Ciclo de pago en tres fases: solicitud, confirmacion y persistencia** | Aceptada | Las tres deben coincidir en usuario, importe y moneda; si difieren, ANOMALY y no se acredita. Primera respuesta gana, positiva o negativa; las posteriores se cancelan y quedan registradas. Tabla propia `payment_cycles` y no `payments`, porque `Payment.orderId` es obligatorio con FK a Order y los depositos de wallet no tienen orden. | `payment-cycle.service.ts`, `schema.prisma` | PT-080 |
| ADR-031 | **Via rapida (webhook) + via garantizada (consulta periodica)** | Aceptada | Un pago cobrado cuya notificacion no llega se perdia en silencio: ocurrio con 180 MXN reales el 2026-07-26. Un job consulta a la pasarela por la referencia de las solicitudes abiertas, con retroceso exponencial (1min..12h) y expiracion a `PAYMENT_EXPIRATION_HOURS` (72, variable ya configurada y sin uso). Al vencer: EXPIRED, se asume no resuelto, no acredita. Elimina ademas la dependencia de un tunel publico en desarrollo. | `payment-reconciliation.service.ts` | PT-080 (F-04) |
| ADR-032 | **Registro de pasarelas por inyeccion; el nucleo no conoce proveedores** | Aceptada | Anadir una pasarela exigia cinco ediciones en la logica de transaccion y quitarla no tenia camino. Cada adaptador declara clave y alias, y normaliza su propio importe. Se revive el puerto `IPaymentProvider` de CORE, evolucionado con validacion **asincrona**: la Fase A demostro que la validacion difiere por formato dentro del mismo proveedor, algo que el puerto original no contemplaba. Sustituye a ADR-013 en el punto de los proveedores fijos. | `payment-provider.registry.ts`, `core/integrations` | PT-080 |
| ADR-033 | **La capa de use-cases de CORE no se adopta** | Aceptada | Cierra la parte aspiracional de ADR-008 y **AUD-012**. Los cuatro use-cases estaban documentados pero su fuente no existe en el repositorio (solo quedaba un `dist/` ignorado por git, de un checkout anterior), y los contratos de repositorio no los referencia nadie. El flujo que gobernarian —orden a PAID y credito CREDIT_SALE al vendedor— ya funciona en `wallet.service.ts:418` y `auction-scheduler.service.ts:152`: adoptarlos reescribiria una ruta de dinero que funciona, obligaria a cuatro adaptadores de repositorio que nadie pidio y anadiria riesgo de regresion en la liquidacion de subastas. Los contratos se conservan marcados como previstos-no-adoptados. **Criterio para revisar la decision**: cuando exista mas de una implementacion de persistencia, o cuando el dominio deba probarse sin base de datos. Contraste con PT-080, que si revivio `IPaymentProvider` porque habia una necesidad real. | `core/src/contracts/index.ts`, `06-Backend-Architecture` | PT-084 (AUD-012) |
| ADR-034 | **Un pago puede no tener orden** | Aceptada | `Payment.orderId` y `RefundRequest.orderId` eran obligatorios con FK a `Order`, y un deposito de wallet no tiene orden: nadie podia escribir `payments` y el panel financiero del admin, que la consulta en seis sitios, mostraba ceros. Pasan a opcionales, con `reference` al ciclo. El registro es contable, no fuente de verdad del dinero: si falla, la acreditacion sigue adelante. | `schema.prisma`, `payment-cycle.service.ts` | PT-085 (TD-008) |
| ADR-035 | **Traza completa de cada transaccion de pago, con credenciales redactadas** | Aceptada | De cada pago queda constancia de por donde paso y que datos se enviaron: solicitud, llamadas salientes con endpoint/estado/duracion, notificaciones con cabeceras, validacion de firma, decision del ciclo y acreditacion con saldos. Se extiende `payment_cycle_events` en vez de crear tabla nueva. `PaymentTraceService` es el punto UNICO de escritura y la redaccion vive dentro: la seguridad no puede depender de que ningun llamante se olvide. **Se redactan las credenciales** — coherente con `AuditEvent.payload`, ya documentado como «whitelisted data only»; guardar el Authorization convertiria la tabla en un almacen de secretos reutilizables. Lo redactado se MARCA. La traza nunca bloquea el pago. | `payment-trace.service.ts`, `schema.prisma` | PT-086 |
| ADR-036 | **Las garantias de un pago pertenecen al registro, no a un proveedor** | Aceptada | PT-080 generalizo el *enrutado* (que adaptador atiende) y dejo particulares las *garantias* (que se le promete a cada pago): traza, via garantizada y rechazo con 401 seguian atadas a Mercado Pago, y PayPal no tenia ninguna de las tres. `findPayment(ctx)` sube al contrato como **opcional a proposito**: cada pasarela busca con lo que tiene —Mercado Pago por nuestra referencia, PayPal por el id de orden, porque no ofrece busqueda por `custom_id`— y que sea opcional deja explicito quien tiene la garantia y quien no, en vez de esconderlo tras un `null`. El reconciliador resuelve por registro y ya no conoce pasarelas. | `payment-provider.interface.ts`, `payment-reconciliation.service.ts`, `paypal.provider.ts`, `schema.prisma` | PT-087 (F-06/F-07/F-08) |
| ADR-037 | **En Orders v2 el sondeo captura, no solo consulta** | Aceptada | A diferencia de Mercado Pago, en PayPal el dinero **no se mueve al aprobar**: una orden `APPROVED` esta autorizada pero sin cobrar, y si nadie la captura no hay pago. La via garantizada de PayPal captura la orden aprobada que ninguna notificacion capturo. Verificado con 321.50 MXN reales que quedaron aprobados y en el limbo el 2026-07-27. | `paypal.provider.findPayment` | PT-087 (F-07) |
| ADR-038 | **Un ciclo no puede declararse cerrado antes de que el dinero llegue al usuario** | Aceptada | `evaluate()` marcaba SETTLED en cuanto la pasarela confirmaba, y `payments` registraba COMPLETED; si acreditar fallaba despues, el sistema quedaba afirmando que el pago estaba resuelto mientras el saldo no existia, y la via garantizada no volvia a mirarlo nunca porque solo recoge ciclos en REQUESTED. Ahora un fallo de acreditacion **reabre** el ciclo. Es seguro porque la reserva de deduplicacion se libera en el mismo camino de error. Observado en real: capturados 321.50 MXN, ciclo SETTLED, `payments` COMPLETED, usuario sin nada. | `payments.service.ts`, `payment-cycle.service.reopenForRetry` | PT-087 (F-09) |
| ADR-039 | **El deposito crea el monedero si no existe** | Aceptada | El monedero se creaba de forma perezosa al consultarlo, y `deposit()` exigia que ya existiera: un usuario que nunca abrio su monedero no podia recibir un deposito. Es peor por la via garantizada, que corre en un cron y no tiene a nadie navegando que provoque la creacion perezosa. La creacion va dentro de la misma transaccion que el asiento: o se crea y acredita, o no ocurre ninguna de las dos. | `wallet.service.deposit` | PT-087 (F-10) |
| ADR-040 | **El asiento contable es idempotente por referencia** | Aceptada | Una solicitud es un pago: `Payment.reference` pasa a UNICA y el asiento se escribe con `upsert`. Sin ello, un ciclo reabierto por ADR-038 dejaba dos filas del mismo cobro: el ledger decia 321.50 y el panel financiero 643.00. En PostgreSQL varios NULL no colisionan, de modo que los pagos de orden —sin referencia— no se ven afectados. **Una BD existente puede tener duplicados de antes de esta correccion: hay que deduplicar antes de crear el indice.** | `schema.prisma`, `payment-cycle.writePaymentRow` | PT-087 (F-12) |
| ADR-041 | **El dominio publico es una sola variable, y las URLs se derivan** | Aceptada | `CLIENT_URL` estaba escrito en tres sitios con dos valores distintos —`client.localhost` en `src/api/.env`, `client.ironloot.local` incrustado en `docker-compose.yml`— y cada adaptador de pago tenia ademas su propio valor por defecto (`5173`, `5175`). `docker-compose` deriva ahora `BASE_URL`/`CLIENT_URL`/`COOKIE_DOMAIN` de `PUBLIC_SCHEME` y `PUBLIC_DOMAIN`. Cambiar de entorno es cambiar dos variables, no perseguir URLs por el arbol. | `.env`, `docker-compose.yml`, `return-urls.ts` | PT-088 (F-17/F-18) |
| ADR-042 | **Una sola ruta de retorno para todas las pasarelas** | Aceptada | Mercado Pago volvia a `/wallet/success`, PayPal a `/wallet/deposit-success`, HeyBanco a `/wallet/deposit-cancel` y Stripe a `/checkout/success`: **ninguna existia** en CLIENT, de modo que un pago real acababa en 404 despues de haber cobrado. Ahora hay una, `/wallet/deposit/return`, y el estado viaja como parametro. | `return-urls.ts`, `app.controller.ts` (CLIENT) | PT-088 (F-16) |
| ADR-043 | **El `status` de la URL de retorno no es fuente de verdad** | Aceptada | Lo escribe el navegador: cualquiera puede cambiar `status=failure` por `status=success`. La pagina lo usa para el primer pintado y pide la verdad a `GET /payments/status/:reference`, que solo responde al dueno y trata un deposito ajeno **como inexistente** (decir «no es tuyo» confirmaria que existe). Un ciclo abierto se informa **pendiente**, nunca fallido: efectivo y SPEI tardan horas, y decir «fallo» provocaria un segundo pago. | `payment-cycle.statusFor`, `deposit-return.html` | PT-088 |
| ADR-044 | **El dominio de desarrollo es `ironloot.local`, no `localhost`** | Aceptada | Los navegadores **rechazan** una cookie con `Domain=.localhost` por ser dominio de uso especial (RFC 6265), de modo que la sesion no cruzaria de BASE a CLIENT. Con `localhost:<puerto>` el problema no se veia porque el puerto no delimita cookies: la sesion se compartia por accidente, y el fallo aparecio justo al pasar a subdominios. `ironloot.local` reproduce ademas la forma de produccion. Exige entradas en el fichero hosts, ya documentadas. | `.env`, `.env.example` | PT-088 (F-18) |
| ADR-045 | **Ninguna URL que abandone el sistema cae en un `localhost:<puerto>`** | Aceptada | PT-088 lo resolvio para las URLs de retorno de pago; el mismo defecto seguia en los enlaces de verificacion de correo y reset de contrasena (que se envian a **usuarios reales**), en la URL de webhook entregada a HeyBanco y en las URLs publicas de los ficheros subidos. `public-origins.ts` es la fuente unica de los tres origenes, y su valor de reserva es un **subdominio**, nunca un puerto: un puerto no falla al arrancar, falla en silencio en produccion cuando alguien ya recibio el correo. | `common/config/public-origins.ts` | PT-089 (F-21) |
| ADR-046 | **Las dependencias de CDN se verifican con SRI** | Aceptada | Chart.js (ADMIN) y socket.io (CLIENT) se ejecutaban sin comprobacion de integridad. Un CDN comprometido ejecutaria codigo arbitrario en el **panel de administracion**, el contexto de mas privilegio del sistema. Se anade `integrity` sha384 + `crossorigin`. Verificado con contraprueba: con hash alterado el navegador bloquea. **Coste**: subir de version una libreria obliga a recalcular el hash, o deja de cargar. Es el precio correcto. | `dashboard.html`, `auction/detail.html` | PT-089 (F-19) |

> **Deuda ADR:** la postura **CSRF** sigue sin ADR propia (`AUD-014`, sin verificar). El **mecanismo único de comisión ya no es deuda**: `AUD-005` está corregido desde PT-042 — el cierre resuelve la tasa con `commissionsService.resolveRatePercent(sellerId)`. Queda como deuda **formalizarlo** en una ADR, no implementarlo.
| ADR-048 | **Los `package-lock.json` se siguen por git y se generan dentro del contenedor** | Aceptada | Habia una contradiccion de meses sin decision registrada: `.gitignore` los ignoraba a los tres bajo el rotulo «regenerated by npm» —y no lo son: un lock regenerado en otra plataforma **no es el mismo lock**—, mientras dos estaban seguidos a la fuerza (raiz y `src/api`) y el tercero no. `src/admin` fue el que pago: su arbol solo existia en la maquina donde nacio. **Se retira la regla** en vez de declarar una excepcion, porque una regla que dice lo contrario de lo que el repositorio hace es una trampa para quien la lea y la respete. Alternativa rechazada: dejar de seguirlos — renuncia a la reproducibilidad y rompe el cache de los siete jobs (`cache: 'npm'` resuelve el lock de la raiz). **Y se generan solo en contenedor**: un `npm install` en Windows deja el lock sin los binarios nativos de Linux y el contenedor muere **al arrancar**, no al instalar, dias despues y en otra maquina. Es la tercera vez (PT-129 en musl, PT-135 en gnu); las dos anteriores se cerraron con un parche en un Dockerfile y por eso hubo tercera. Excepcion **declarada y vigilada**: `tests/qa-browser-suite/` instala en el host porque Playwright conduce un navegador real, y su lock tiene cero paquetes por plataforma — una prueba comprueba que siga siendo cero. Nota historica: `5c16af4` metio el lock del API junto a cambios de paginas y de `Dockerfile.dev`, sin decision; la historia **no se reescribe** y queda como ejemplo de por que existe la regla de commits atomicos. | `.gitignore`, `scripts/solo-en-contenedor.js`, `scripts/regenerar-lock.js`, `lock-declara-plataformas.spec.ts`, `solo-en-contenedor.spec.ts` | PT-135 |
| ADR-047 | **Se retiran los endpoints de pago sin llamantes** | Aceptada | `POST /wallet/deposit` y `POST /payments/checkout` no los invocaba ningun cliente: el unico llamante en todo `src/` eran sus propios tests e2e. El deposito real es `POST /payments/initiate` (ADR-034..ADR-040), que el portal llama desde `pages-wallet-deposit.js`. `/wallet/deposit` **acreditaba dinero** a partir de un `referenceId` elegido por el cliente, verificado contra la pasarela: superficie que mueve saldo, sin uso, sin cobertura y sin mantenimiento. Se retira en vez de corregirse — H-018 describia que un fallo de la pasarela salia como 500 desde ahi, y pulir el manejo de errores de una puerta que sobra es trabajo perdido. **`WalletService.deposit()` se conserva**: es lo que usa `creditWallet` en la via real. | `wallet.controller.ts`, `payments.controller.ts`, `endpoints-legados-retirados.spec.ts` | PT-133 (H-018) |
| ADR-049 | **`docs-v2/` es la documentacion oficial; `enterprise-documentation/` queda como contrato de agente** | Aceptada | Habia **dos arboles declarandose mutuamente sustitutos**. `docs/enterprise-documentation/` (Foundation Protocol, 23-jun) decia de si mismo *«toca regenerar. Decision del humano»*; `docs-v2/` (23-jul) se declaraba *«la unica fuente de verdad»*. Y `CLAUDE.md` —el documento que gobierna a todo agente— citaba al **primero diez veces**, todas vinculantes, y al segundo **una**. El coste no era teorico: los commits `6decb1a` y `4f40358` escriben en los dos, asi que **cada PT pagaba doble**, y la divergencia era cuestion de tiempo (H-016 ya demostro que una cita precisa que se desplaza se lee con confianza y es falsa). **Decision**: `docs-v2/` es la documentacion del producto, para personas. `enterprise-documentation/` se acota al **contrato de agente**: `11-Conventions.md` (las veinte RULE), `10-Technical-Debt.md` (registro TD-XXX, con guarda desde PT-103) e `inventory/`. Los otros nueve se archivan **con un mapa documento a documento**, porque archivar sin mapa convierte el archivo en un cementerio. **No es «uno gana»**: son dos funciones que estaban mezcladas — una describe el producto, la otra le dice a un agente que no puede romper. El inventario de solapamiento (evidencia de PT-141) verifico la premisa de `docs-v2` documento a documento en vez de asumirla: **es cierta para nueve de doce**, y los tres restantes no tienen equivalente y no se archivan. **Alternativa rechazada**: retirar `enterprise-documentation/` entero — dejaria sin sitio las RULE-NN, que son el contrato operativo, y romperia la guarda de deuda. | `CLAUDE.md`, `docs/enterprise-documentation/`, `11-Conventions.md` | PT-141 |
| ADR-050 | **La recepcion la confirma quien recibe, y el reloj del holdback cuelga de esa confirmacion** | Aceptada | Hasta PT-174 **todo** cambio de estado del pedido era del vendedor, y el cron liberaba el holdback en cuanto el pedido estaba `DELIVERED`: **el vendedor marcaba entregado su propio envio y liberaba su propio dinero**, sin enviar nada y sin que nadie confirmara. El holdback protege al comprador durante la ventana de disputa, y lo podia desactivar la unica parte de la que protege. Ahora el vendedor declara `SHIPPED` y **solo el comprador** confirma `DELIVERED` (el vendedor recibe 403), y el neto espera `SETTLEMENT_HOLDBACK_HOURS` **desde `shipment.deliveredAt`**, no desde el estado ni desde la ultima modificacion de la fila — que era el defecto de H-011. **Consecuencia**: aparece una segunda mentira posible, la del comprador que **niega** la recepcion para retener el dinero del vendedor; se cubre conservando el vencimiento a los `DISPUTE_WINDOW_DAYS`, que antes existia por accidente como el otro brazo de un `OR` y ahora es una regla con su prueba. **Y la transicion pasa por `OrderStateMachine`**: existia en `@ironloot/core` y `shipments.service.ts` escribia `order.status` a mano — dos puertas al mismo estado y solo una con cerradura. | `shipments.service.ts`, `auction-scheduler.service.ts`, `35-cierre-y-liquidacion.js` | PT-173/PT-174/PT-175 |
| ADR-051 | **Un servicio compartido no decide que hacen sus llamantes con un fallo** | Aceptada | `EmailService` capturaba cualquier error de envio y no lo relanzaba, con su propia duda escrita al lado: *«Don't rethrow to avoid breaking registration flow?»*. La duda era buena; contestarla **una vez para todos los llamantes** no puede serlo, porque la respuesta correcta es distinta en cada uno y el servicio no sabe a cual sirve. Lo que absorbia eran **tres capas de recuperacion**: el `catch` del worker de la cola, su contador de intentos y los reintentos de BullMQ — un envio fallido marcaba el trabajo como **completado**. **Decision**: el servicio propaga y cada llamante declara su decision **con el motivo escrito al lado**. Reenvio de verificacion y cola **propagan**; registro **captura** (la cuenta ya existe y hay via de reenvio); recuperacion de contrasena **captura**, porque su respuesta es opaca a proposito y propagar convertiria una caida del SMTP en un **oraculo de enumeracion** (500 para las direcciones que existen, 200 para las que no). **Consecuencia incomoda**: habia una prueba **verde** sosteniendo el defecto —`should not throw when mailerService fails`, dos veces—, misma forma que el caso de `BASE_URL` que exigia la reserva `localhost:5174` hasta PT-089. Una prueba puede ser el mecanismo que mantiene vivo un defecto. | `email.service.ts`, `auth.service.ts`, `notification-queue.worker.ts`, RULE-36 | PT-183 (H-032) |
| ADR-052 | **Toda llamada a un tercero declara su tope de espera, y la asimetria es del dominio** | Aceptada | Antes de esta jornada, **dos** ficheros del API declaraban un tope — y los dos se escribieron el mismo dia. El transporte de correo no declaraba ninguno, asi que nodemailer aplicaba el suyo: **dos minutos**. Medido con el SMTP parado, el reenvio de verificacion y **el registro** —la primera pantalla del producto— se colgaban **121 s**. Y las **seis** llamadas de los tres adaptadores de pasarela usaban `fetch` sin `signal`, sin un `AbortController` en todo el directorio, incluida la que **captura** en PayPal. **Decision**: `MAIL_TIMEOUTS_MS` (5/5/10 s) y `GATEWAY_TIMEOUTS_MS` (consultar 8 s, **operar 20 s**). Los valores **se derivan** de lo que el sistema ya espera de un tercero (5 s de Google, 2 s de Redis), no se eligen. **La asimetria es del dominio, no del transporte**: consultar puede cortarse pronto porque la via garantizada volvera a preguntar; **crear o capturar** no, porque abandonar algo que quizas se completo al otro lado deja un cobro sin saber que paso. **Consecuencia**: un tope agotado **no es un rechazo** de la pasarela — PT-179 ya establecio que un 4xx del tercero no es una averia nuestra, y un timeout tampoco es un «no». | `mail-timeouts.ts`, `gateway-timeouts.ts`, `notifications.module.ts` | PT-183/PT-184 (H-033, H-034) |
| ADR-053 | **Ninguna variable de conexion tiene reserva, en ninguno de los cuatro servicios** | Aceptada | ADR-045 resolvio esto para las URLs que **salen** del sistema; el mismo defecto seguia en las variables que dicen **a donde llamar**. Medido: **seis** reservas a `localhost` —una en el API, tres en BASE, tres en CLIENT; ADMIN estaba limpio—, y las dos mas caras eran las del **proxy del BFF**: sin `API_URL`, el sitio manda *todas* sus llamadas a su propio contenedor y **arranca `healthy` sin funcionar**. El cerrojo distribuido conservaba `process.env.REDIS_URL \|\| 'redis://localhost:6379'` a traves de PT-137 y PT-147. **Decision**: `variableObligatoria()` **aborta nombrando la variable**; el API usa `clientOrigin()` en vez de duplicar la reserva. **Y lo que vale mas que el arreglo**: la guarda de RULE-17 comprobaba que las variables estuvieran **declaradas**, no que carecieran de **reserva** — que es la mitad que la propia regla llama, en negrita, el problema. Habia una guarda con el nombre correcto **mirando otra cosa**. **Reserva que se conserva a proposito**: `public-origins.ts` mantiene el **subdominio de desarrollo** por ADR-045; es discutible —un despliegue que olvide `BASE_URL` mandaria correos a un dominio que para el usuario no existe— y queda como decision vista, no como cabo suelto. | `variable-obligatoria.ts` (BASE y CLIENT), `distributed-lock.service.ts`, `conexiones-sin-reserva.spec.ts` | PT-185/PT-186 (H-035) |
| ADR-054 | **Un registro append-only necesita un indice generado, y un BUG que el agente cierra no esta cerrado** | Aceptada | `HISTORY.log` es append-only, asi que la linea `Status:` de una entrada **es historica**: el cierre se anota mas abajo, en un bloque de VoBo. Consecuencia medida: **102 entradas dicen `VALIDATION_PENDING` estando cerradas**, y eso hizo que se reportara **PT-147 como pendiente** llevando horas cerrado — el fichero lo decia, y se repitio el fichero en vez de medirlo. **Decision**: un indice **generado** al final del propio fichero (`npm run indice:estado`). **Anade, no reescribe**: reescribir las 102 lineas se leeria mejor y borraria el momento en que se supo cada cosa, que es lo que la regla append-only existe para preservar. **Segunda mitad, y vale mas**: un BUG con `Status: DONE` sin bloque de VoBo que lo nombre **no esta cerrado**. Su primera ejecucion encontro cinco BUG escritos `DONE` directamente por el agente; el VoBo estaba dado, pero **un cierre sin constancia de quien lo autorizo es indistinguible de uno auto-otorgado**, y esa distincion es la razon de ser de FDGE STATE 6. | `indice-de-estado.ts`, `indice-de-estado-al-dia.spec.ts`, RULE-37 | PT-187 |
| ADR-055 | **v1.0 no afirma que su fiabilidad operacional este demostrada** | Aceptada | El checkpoint D5 dio cuatro veces el mismo resultado: `SIN_DATOS` por muestra insuficiente. Hacen falta **20 ciclos de pago resueltos** y hay **2**, y el minimo no se eligio — sale del propio umbral verde: con `>= 95 %`, un solo fallo entre `n` cumple `(n-1)/n >= 0.95` solo si `n >= 20`. Bajarlo devolveria el defecto de H-028, donde una tasa sobre dos casos capaba la clase por aritmetica. Y cada ciclo exige **aprobacion manual en el sitio del proveedor**: fabricarlos sin pasar por la pasarela mediria nuestro codigo, no la fiabilidad de las pasarelas. **Decision**: declararlo, con el mecanismo de H-005/CFDI — el sistema **tiene** los mecanismos (via garantizada, reapertura del ciclo, asiento idempotente, reintentos) y cada uno esta verificado; lo que no existe es la **serie**. **Consecuencia, y es lo que hace honesta la declaracion**: la cobertura de D5 se queda en 0 % y la Confianza en 91.0. **El numero no mejora por declararlo** — a diferencia de H-005, donde la aceptacion si subio D1 porque el producto dejo de prometer la factura. **Reapertura declarada**: con volumen real de produccion los 20 ciclos aparecen solos y esta ADR caduca sin otra decision. **Lo que NO dice**: que el sistema sea poco fiable. Dice que **no se puede afirmar que lo sea**. | `PTSA/Fases/F-1_Declaracion_Valor.md § U-007`, `reliability-check.ts` | S-010 (D5) |

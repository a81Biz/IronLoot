{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "//": "PT-094 (TD-013) — Igual que nest-cli.json PERO SIN borrar la salida.",
    "//2": "El de produccion mantiene deleteOutDir: true, que es lo correcto para un build:",
    "//3": "no debe quedar codigo viejo en el dist que se despliega.",
    "//4": "En el contenedor de desarrollo ese borrado abria una ventana fatal: nest elimina",
    "//5": "dist/, recompila, y con los montajes de Windows `node dist/main` se disparaba antes",
    "//6": "de que la compilacion aterrizara. Node moria, se llevaba el watcher por delante, y",
    "//7": "el contenedor quedaba unhealthy con `Cannot find module '/app/dist/main'`.",
    "//8": "Consecuencia: `docker restart ironloot-admin` dejaba el panel caido.",
    "deleteOutDir": false
  }
}

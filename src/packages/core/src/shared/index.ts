// Shared DTOs and value types used across domain, application, and integration layers.

export * from "./pagination.dto";
// PT-191 (AUD-012) — `money.dto.ts` retirado con el VO `Money`. Motivo en `domain/index.ts`.

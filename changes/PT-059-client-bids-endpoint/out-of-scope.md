# PT-059 — Out of scope
- Paginación server-side de `my-history` (la API devuelve todo; `page` queda sin efecto server-side) — enhancement futuro.
- No se añaden endpoints a la API.
- BUG-QA-N01 (wallet) es PT-058.

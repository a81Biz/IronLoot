---
id: E-001
tipo: codigo
origen: src/api/src/modules/bids/bids.service.ts
lineas: 98-108
capturada: 2026-06-23
fingerprint: SHA256-estructural-bids-service-extension-ms-hardcoded-300s
estado: VALID
---

# E-001 — Soft-close extension hardcoded a 5 minutos en BidsService

## Contenido capturado
```typescript
// Variables for WebSocket (needs to be available outside tx)
const EXTENSION_MS = 5 * 60 * 1000;
let timeRemaining = 0;
let newEndsAt = auction.endsAt;

// Calculate extension before tx/inside tx logic if needed, but for logging/events we need it out here.
// But `auction.endsAt` is fixed snapshot. `now` is fixed.
// So we can calculate it here safely since we hold the optimistic lock or just assume standard flow.
timeRemaining = auction.endsAt.getTime() - now.getTime();
if (timeRemaining < EXTENSION_MS) {
  newEndsAt = new Date(auction.endsAt.getTime() + EXTENSION_MS);
}
```

## Observación
`EXTENSION_MS` está hardcodeada en `5 * 60 * 1000` = 300,000 ms = 5 minutos. Cuando un comprador coloca una puja con menos de 5 minutos restantes en la subasta, el sistema extiende el cierre por 5 minutos adicionales (no por 120 segundos como documenta el PRD y la variable de entorno `AUCTION_SOFT_CLOSE_WINDOW_SEC`). La constante no lee de ninguna fuente de configuración.

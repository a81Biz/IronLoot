---
id: H-005
tipo: DOMAIN_RULE
dimension: D1
producto_id: P-009
fase_detectada: F6
estado: ABIERTA
severidad: ALTA
impacto: 2
probabilidad: 3
penalizacion: 15
confidence: 98
evidencias: [E-007, E-009]
---

# H-005 — CFDI/PAC integration es un stub — IronLoot no emite facturas fiscales (compliance SAT)

## Descripción
El módulo `CfdiModule` / `CfdiService` gestiona el modelo de datos `cfdi_records` en la base de datos pero no realiza ninguna integración real con un Proveedor Autorizado de Certificación (PAC) del SAT mexicano. La emisión de Comprobantes Fiscales Digitales por Internet (CFDI) es un stub declarado.

Este gap está documentado en PRD Out-of-Scope, Foundation Protocol TD-001 (HIGH), HANDOFF.md, y el inventory de servicios — es una limitación conocida y explícita de v1.0.0.

## Evidencia Clave
- **E-007**: `docs/implementation/HANDOFF.md` TD-001 + `docs/enterprise-documentation/02-PRD.md` Out-of-Scope section + `inventory/services.md` CfdiService annotation

## Impacto en el Dominio
**CR-011 violada**: La regla de dominio establece que toda transacción gravable debe producir un `CfdiRecord` válido (folio, UUID, XML). Actualmente las transacciones no generan CFDI válidos aunque el registro en base de datos exista.

En México, el Artículo 29-A del Código Fiscal de la Federación (CFF) y la NOM del SAT exigen CFDI para operaciones de compra-venta. IronLoot como plataforma de subastas que intermedia la venta de bienes entre particulares está sujeta a obligaciones de facturación electrónica al superar umbrales de operación o con compradores que lo requieran.

**Sin CFDI real**: La empresa no puede operar legalmente a escala comercial en México. Los compradores/vendedores no pueden deducir sus operaciones. Hay riesgo de multas del SAT.

## Riesgo Calculado
- Impacto: 2 (alto-dominio — compliance fiscal es bloqueador para operación comercial)
- Probabilidad: 3 (alta — el gap es permanente hasta que se implemente un PAC real)
- Risk_bruto: 6

## Recomendación
Priorizar la integración con un PAC certificado por el SAT (ej: Edicom, SIFEI, Finkok) antes del lanzamiento comercial. El modelo de datos ya existe (`cfdi_records`). El trabajo es implementar `CfdiService.emitCfdi()` con la API del PAC seleccionado, almacenar el XML firmado y el UUID del SAT, y enlazar el `cfdi_record` con los `orders` correspondientes.

**PT sugerido**: FEATURE MAYOR — requiere propuesta completa, contratos con PAC, análisis fiscal. Branch: `feature/PT-XXX-cfdi-pac-integration`


---

## Revisión — DS-004 (2026-07-27)

**Motivo**: `[R34]` — revalidación de evidencias en delta sync.

### Estado de las evidencias

| Evidencia | Fuente | Estado | Acción |
|---|---|---|---|
| E-007 | `HANDOFF.md:26-27` | **STALE** | El fichero se ha reescrito; esas líneas contienen hoy texto de PT-107 |
| E-007 | `inventory/services.md:44` | VALID | Intacta |
| E-007 | `02-PRD.md:142` | VALID | Intacta |
| **E-009** | `src/api/src/modules/cfdi/` + las dos anteriores | **VALID** | Recaptura, con observación directa del código |

### Verificación del hecho de fondo

Se comprobó en el código, no en la documentación (A5):

```
$ ls src/api/src/modules/cfdi/          → cfdi.module.ts, cfdi.service.ts
$ grep -c "fetch(|axios|http\." …/cfdi/ → 0
```

**Cero llamadas HTTP salientes.** No hay integración con ningún PAC. El hallazgo **sigue siendo
cierto** y permanece `ABIERTA`, con la misma severidad.

Un matiz que la recaptura hace visible: el servicio **no se declara stub en ningún comentario del
código**. La única declaración de que lo es vivía en la documentación — lo que significa que si
alguien borrara esa línea del inventario, nada en el código diría que el CFDI no funciona. Es la
misma clase de silencio que hizo invisible a F-34, en otro sitio.

### Bloqueo

Sin cambios: exige **contratar un PAC certificado ante el SAT**. Ninguna decisión técnica lo
desbloquea. Sigue siendo el único hallazgo activo y el que impide que D1 llegue a 100.


---

## Revisión — PT-113 (2026-07-27): la causa raíz estaba mal

Este hallazgo lleva desde el 23-jun declarado «bloqueado por contratar un PAC ante el SAT». PT-113
comprobó esa causa raíz y **no se sostiene**: si mañana apareciera el contrato, seguiría sin poder
emitirse un CFDI.

### Lo que sí estaba hecho, y este hallazgo no reconocía

El modelo `cfdi_records` está completo, hay interruptor `CFDI_ENABLED` apagado por defecto, claves
de configuración, mensajes de error accionables, y **el contrato `ICfdiPacProvider` ya existe** en
`@ironloot/core`. No es un hueco: es un adaptador sin implementación concreta.

### Lo que falta, y no depende del SAT

1. **`CfdiData` tiene 6 campos.** Un CFDI 4.0 exige además `UsoCFDI`, `RegimenFiscal` de emisor y
   receptor, `LugarExpedicion`, `MetodoPago`, `FormaPago`, `ClaveProdServ`, `ClaveUnidad` e
   impuestos. Implementarlo tal cual daría un **rechazo del PAC**.
2. **Los datos fiscales no se capturan**: en toda la base hay **una** columna fiscal,
   `profiles.rfc`.
3. **No está decidido quién emite la factura.** Se buscó en el PRD y en F-1: no está. De esa
   decisión —el vendedor, IronLoot por cuenta del vendedor, o sólo la comisión— dependen los
   campos, el modelo de datos y **qué contrato firmar con el PAC**.

### Causa raíz corregida

> El bloqueo real es **una decisión de dominio que nadie ha tomado**, no un trámite con un tercero.
> El PAC es la última pieza, no la primera.

Orden real: (1) decidir quién emite → (2) ampliar el contrato → (3) capturar los datos → (4) PAC.

La pregunta queda escrita en **F-1 § Update U-005**, donde se buscó y no estaba, con las tres
opciones y lo que exige cada una. Y queda dicho que **`CR-011` no es auditable** mientras no nombre
al obligado: una regla que dice «toda transacción gravable debe producir un CFDI» sin decir de
quién es la obligación no se puede verificar.

**Sigue ABIERTA.** Severidad sin cambios. Registrado como **F-40**.

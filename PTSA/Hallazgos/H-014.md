---
id: H-014
tipo: BUG
dimension: D2
producto_id: null
fase_detectada: F5
estado: CERRADA
severidad: CRITICA
impacto: 4
probabilidad: 2
penalizacion: 30
confidence: 98
evidencias: [E-025, E-017]
revisiones: [PT-127]
sesion_deteccion: S-002
---

# H-014 — Las migraciones no reproducen el esquema; el único artefacto desplegable está roto

## Descripción

La base de datos que la auditoría ha certificado durante nueve sesiones la construye
`prisma db push --accept-data-loss` en el arranque del contenedor (`entrypoint.dev.sh:52`).
`db push` no escribe `_prisma_migrations`, y esa tabla **no existe** en la base real.

Consecuencia: **las 23 migraciones de `prisma/migrations/` no se han ejecutado nunca**. Aplicadas a
una base limpia producen un esquema distinto del certificado, y la aplicación no funciona sobre él:

```
  FALLA  userPaymentMethod.findMany   -> la columna user_payment_methods.type no existe
  FALLA  paymentCycle.findMany        -> la columna payment_cycles.provider_ref no existe
  FALLA  accountVerification.findMany -> la tabla account_verifications no existe
  OK     payment.findMany
```

Además, `payments.reference` es un índice **corriente** en las migraciones y **único** en el esquema
real. Esa unicidad es la que CLAUDE.md declara como garantía de que un reintento de acreditación no
duplica el asiento contable.

## Evidencia

`E-017` — migraciones aplicadas a base sombra, comparación de esquemas, sondas del cliente Prisma.

## Por qué es CRITICA

§13.4: *«riesgo de seguridad/integridad grave»*. El sistema no tiene hoy ningún camino verificado
para llevar su esquema a otro entorno. El `Dockerfile` de producción termina en
`CMD ["node", "dist/main"]` sin aplicar esquema alguno, y `ci.yml` no tiene job de despliegue: el
único artefacto candidato son las migraciones, y están rotas.

## Por qué la probabilidad es 2 (Posible) y no 4

**Hoy no hay despliegue productivo.** Nada está fallando ahora mismo. El daño se materializa en el
momento —cierto, pero no fechado— en que alguien reconstruya un entorno desde las migraciones: un
despliegue, un entorno de staging, un CI que aplique esquema, o un desarrollador nuevo si el
`db push` fallara y cayera al respaldo de la línea 54 (que a su vez devuelve `P3005`).

Riesgo = 4 × 2 = **8 (ALTO)**.

## Lo que este hallazgo NO afirma

- **No** afirma que se haya acreditado ningún depósito por duplicado. En la base real la unicidad
  existe y `R-5.1c` pasa (E-019). La pérdida de la restricción se observó en el artefacto, no un
  duplicado en datos.
- **No** identifica qué migración concreta introdujo cada divergencia.
- **No** evalúa `--accept-data-loss` como incidente: es una bandera peligrosa en un arranque
  automático, pero no se observó ninguna pérdida de datos.

## Vías de corrección (para quien decida, no decididas aquí)

1. **Rebasar el historial**: `prisma migrate diff` desde las migraciones al esquema, generar una
   migración de reconciliación, y marcar las 23 como aplicadas (`migrate resolve --applied`).
2. **Empezar de cero**: colapsar las 23 en una migración inicial que sea, por construcción, el
   esquema actual.
3. **Cerrar el hueco de proceso**, en cualquiera de los dos casos: que algo compruebe en CI que
   migraciones y esquema no divergen. Sin eso, esto vuelve — es el mismo patrón que PT-118 arregló
   para las dependencias.

La opción 1 conserva el historial; la 2 lo pierde y es más limpia. **Es una decisión de plataforma,
no del auditor.**

## Estado

`ABIERTA`. Tipo `BUG`: `[R44]` prohíbe al agente cerrarlo. Requiere corrección y validación humana.

---

## Revisión — PT-127 (2026-07-27): CORREGIDA

**Estado: `ABIERTA` → `CORREGIDA`.** No se cierra: es tipo `BUG` y `[R44]` reserva el cierre al
humano.

### Qué se hizo

Vía **B** (colapsar), decidida en el Proposal Gate con evidencia: un solo autor en todo el
historial, `master` == `origin/master`, y **ninguna de las 57 ramas** contenía las migraciones que
faltaban. No había historial que conservar — esas 23 no se ejecutaron en ningún sitio.

| | |
|---|---|
| Las 23 migraciones | sustituidas por una inicial **generada**, no escrita |
| `entrypoint.dev.sh` | `db push --accept-data-loss` → `migrate deploy`, sin respaldo silencioso |
| El mecanismo que faltaba desde PT-037 | `audit:schema` + job de CI **sin `needs`** |

### Verificado en la fuente real

```
migrate deploy sobre base limpia   ->  All migrations have been successfully applied
migrate diff --exit-code           ->  No difference detected · EXIT 0
sondas del cliente Prisma          ->  4 de 4   (antes 1 de 4)
payments.reference                 ->  payments_reference_key UNIQUE   (antes indice corriente)
migrate status sobre ironloot_db   ->  Database schema is up to date!
datos de auditoria                 ->  wallets 4 · ledger 15 · payments 1 · bids 3  IDENTICO
```

**La garantía que faltaba existe ahora en el artefacto desplegable**: `payments.reference` es único,
que es lo que impide que un reintento de acreditación duplique el asiento contable.

### El control, probado en los dos sentidos

Campo nuevo en `schema.prisma` sin migración → `audit:schema` **FALLA**, nombra la columna y dice
qué hacer. Revertido → **PASA**. Un control que nadie ha visto fallar no es un control.

### Lo que NO se comprobó, dicho

El escenario C3 —que un fallo de migración detenga el arranque— **no se ejecutó**. Exigía corromper
la única migración existente con la base de desarrollo ya baselineada contra ella, y eso arriesgaba
el dato real que sostiene la validación de 11 productos. Queda como **comprobación pendiente**, no
como comprobación aprobada.

### Una lección que este PT casi repite

`src/api/scripts/` no está montado como volumen. Tras cambiar el entrypoint, el contenedor **seguía
ejecutando `db push`** desde la imagen. Se detectó leyendo el log —decía «The database is already in
sync», que es el mensaje de `db push`— y no el fichero.

Fiarse del fichero habría cerrado este PT afirmando algo falso. **Es exactamente el defecto que el
PT viene a cerrar**, a punto de repetirse dentro de su propia corrección.

Evidencia completa: `docs/implementation/evidence/PT-127/`.


---

## Validación humana — 2026-07-28

**Estado: `CORREGIDA` → `CERRADA`.**

VoBo del humano, instrucción literal: *«retira los endpoints, abre un navegador y valida los
hallazgos completos»*. `[R44]` reserva el cierre de `BUG`/`DOMAIN` a la persona; ésta es su
autorización, y queda citada aquí para que el cierre sea trazable y no una decisión del agente.

La verificación por navegador está en `E-025`: suite QA completa (**127 comprobaciones, 0 fallos**,
con cobro real en Mercado Pago y orden real aprobada en PayPal) más 9 comprobaciones dirigidas a
los hallazgos, todas PASS.

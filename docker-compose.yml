services:
  # ===========================================
  # Nginx Reverse Proxy (subdomains)
  # localhost       → web:5173
  # api.localhost   → api:3000
  # admin.localhost → admin:3001
  # ===========================================
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: ironloot-nginx
    ports:
      - "80:80"
    depends_on:
      - web
      - api
      - admin
    networks:
      - ironloot-network
    restart: unless-stopped

  # ===========================================
  # Iron Loot Admin Panel (NestJS SSR)
  # ===========================================
  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile.dev
    container_name: ironloot-admin
    ports:
      - "${ADMIN_PORT:-3001}:3001"
    environment:
      - NODE_ENV=development
      - ADMIN_PORT=3001
      - ADMIN_API_URL=http://api:3000
      - ADMIN_API_KEY=${ADMIN_API_KEY:-dev-admin-key}
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
      - ADMIN_SESSION_SECRET=${ADMIN_SESSION_SECRET:-admin-dev-secret-change-me}
    volumes:
      - ./admin/src:/app/src
      - ./admin/views:/app/views
      - ./admin/public:/app/public
      - ./admin/package.json:/app/package.json
      - ./admin/tsconfig.json:/app/tsconfig.json
      - ./admin/nest-cli.json:/app/nest-cli.json
      - /app/node_modules
    depends_on:
      - api
    networks:
      - ironloot-network
    deploy:
      resources:
        limits:
          memory: 512M
    restart: unless-stopped

  # ===========================================
  # Iron Loot Web (NestJS SSR)
  # ===========================================
  web:
    build:
      context: ./web
      dockerfile: Dockerfile.dev
    container_name: ironloot-web
    ports:
      - "${WEB_PORT:-5173}:5173"
    environment:
      - VITE_API_URL=http://api:3000
    env_file:
      - ./api/.env
    volumes:
      - ./web/src:/app/src
      - ./web/public:/app/public
      - ./web/views:/app/views
      - ./web/package.json:/app/package.json
      - ./web/tsconfig.json:/app/tsconfig.json
      # Exclude node_modules
      - /app/node_modules
    depends_on:
      - api
    networks:
      - ironloot-network
    deploy:
      resources:
        limits:
          memory: 1G
    restart: unless-stopped

  # ===========================================
  # Iron Loot API (NestJS)
  # ===========================================
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    container_name: ironloot-api
    ports:
      - "${API_PORT:-3000}:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://${DB_USER:-ironloot}:${DB_PASSWORD:-ironloot_dev}@db:5432/${DB_NAME:-ironloot_db}
      - REDIS_URL=redis://redis:6379
      - LOG_LEVEL=${LOG_LEVEL:-debug}

      - TRACE_ENABLED=true
      - ADMIN_API_KEY=${ADMIN_API_KEY:-dev-admin-key}
      - MAIL_HOST=mailhog
      - MAIL_PORT=1025
      - MAIL_USER=test
      - MAIL_PASSWORD=test
      - MAIL_FROM="Iron Loot <noreply@ironloot.com>"
    # Load additional env vars from project .env into the container
    env_file:
      - ./api/.env
    volumes:
      - ./api/src:/app/src
      - ./api/prisma:/app/prisma
      - ./api/test:/app/test
      - ./api/package.json:/app/package.json
      - ./api/tsconfig.json:/app/tsconfig.json
      - ./api/nest-cli.json:/app/nest-cli.json
      # Exclude node_modules to use container's version
      - /app/node_modules
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - ironloot-network
    deploy:
      resources:
        limits:
          memory: 1G
    restart: unless-stopped

  # ===========================================
  # PostgreSQL Database
  # ===========================================
  db:
    image: postgres:16-alpine
    container_name: ironloot-db
    ports:
      - "${DB_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${DB_USER:-ironloot}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-ironloot_dev}
      POSTGRES_DB: ${DB_NAME:-ironloot_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./api/scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    networks:
      - ironloot-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-ironloot} -d ${DB_NAME:-ironloot_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M
    restart: unless-stopped

  # ===========================================
  # Redis (Cache & Sessions)
  # ===========================================
  redis:
    image: redis:7-alpine
    container_name: ironloot-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      - ironloot-network
    command: redis-server --appendonly yes
    deploy:
      resources:
        limits:
          memory: 256M
    restart: unless-stopped

  # ===========================================
  # Mailhog (Local Email Testing)
  # ===========================================
  mailhog:
    image: mailhog/mailhog
    container_name: ironloot-mailhog
    ports:
      - "1025:1025" # SMTP server
      - "8025:8025" # Web UI
    networks:
      - ironloot-network
    deploy:
      resources:
        limits:
          memory: 256M
    restart: unless-stopped

  # ===========================================
  # pgAdmin (Database Administration) - Optional
  # ===========================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: ironloot-pgadmin
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@ironloot.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - ironloot-network
    depends_on:
      - db
    profiles:
      - tools
    restart: unless-stopped

networks:
  ironloot-network:
    driver: bridge

volumes:
  postgres_data:
    name: ironloot_postgres_data
  redis_data:
    name: ironloot_redis_data
  pgadmin_data:
    name: ironloot_pgadmin_data
